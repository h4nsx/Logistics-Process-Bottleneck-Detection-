# Bắt buộc dùng postgresql+psycopg:// (không dùng +asyncpg — không cài asyncpg)
DATABASE_URL=postgresql+psycopg://postgres:YOUR_PASSWORD@localhost:5432/logistics_db
MAX_UPLOAD_SIZE_MB=10
LOG_LEVEL=INFO
