# Python 3.13 on Windows: use psycopg (binary wheels) instead of asyncpg;
# use recent pydantic so pydantic-core has cp313 wheels (no Rust build).
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
psycopg[binary]>=3.2.0
sqlalchemy[asyncio]>=2.0.36
alembic>=1.14.0
python-multipart>=0.0.9
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
